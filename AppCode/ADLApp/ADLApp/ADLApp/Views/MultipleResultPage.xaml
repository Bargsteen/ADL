<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:controls="clr-namespace:ADLApp.Views;assembly=ADLApp"
             x:Class="ADLApp.Views.MultipleResultPage">
  <StackLayout>
    <StackLayout VerticalOptions="CenterAndExpand">
      <Label Text="{Binding ResultText}" FontSize="30" ></Label>
      <ListView x:Name="answerOptionView" HasUnevenRows="True" ItemsSource="{Binding Path=FeedBackList}" ItemSelected="OnItemSelected" >
        <ListView.ItemTemplate>
          <DataTemplate>
            <ViewCell>
              <StackLayout Orientation="Horizontal" >
                <Label FontSize="25" Text="{Binding Item3}" TextColor="{Binding Item2}"></Label>
                <Label FontSize="25" Text="{Binding Item1}" TextColor="{Binding Item2}"></Label>
              </StackLayout>
            </ViewCell>
          </DataTemplate>
        </ListView.ItemTemplate>
      </ListView>
    </StackLayout>
    <StackLayout VerticalOptions="End">
      <Button Text="Fortsæt" Clicked="OnContinueButtonClicked" BackgroundColor="#66bb6a"></Button>
    </StackLayout>
  </StackLayout>
</ContentPage>